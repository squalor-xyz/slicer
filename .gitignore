# Python
__pycache__/
*.py[cod]
*.egg-info/
build/
dist/
.venv/
venv/

# Tooling caches
.pytest_cache/
.mypy_cache/
.ruff_cache/

# macOS
.DS_Store

# NOTE: .slicer/ is deliberately NOT ignored.
# It is this project's own roadmap state, and .slicer/render/ is generated
# markdown that is meant to be committed and diffed — `slicer check` fails
# when it is stale. Do not add it here.
