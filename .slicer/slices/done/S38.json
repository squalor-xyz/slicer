{
  "id": "S38",
  "title": "Priority and criticality scoring: the score as data",
  "lead": [],
  "depends_note": null,
  "findings_note": "user request; Eisenhower matrix",
  "size": "M",
  "flags": [],
  "trees_note": "slicer",
  "trees_plural": false,
  "sections": [
    {
      "heading": "Why",
      "body": "Order today is manual: position in the index is the priority, set by `move`, and nothing\nsorts. There is no way to record how important or how urgent an item is, so triage lives\nonly in a human's head and in the queue order.\n\nThis slice adds the score as data. Each item gets an **importance** and an **urgency**,\neach **1–3**, defaulting to **2/2** when unset — so the 37 existing items interleave\nrather than all sinking or floating. The base score is **importance×10 + urgency**, which\nranks the Eisenhower quadrants importance-first: Q1 do-now (33) > Q2 schedule (31) > Q3\ndelegate (13) > Q4 drop (11), so important-not-urgent always outranks urgent-not-important.\nA quadrant label is derived for display (a 2 or 3 on an axis is \"high\").\n\n**Manual order stays the stored truth** — `move`, `render`, and the ROADMAP.md order are\nuntouched. The score adds a read-only `list --sort score` view. Propagation and `next`\nare the next slice."
    },
    {
      "heading": "Files",
      "body": "- `src/slicer/model.py` — two fields on `Item`, in the `fields` sub-dict of to_dict/from_dict\n- `src/slicer/ops.py` — `set_fields` known set, `add` constructor, `ONE_LINE_FIELDS` if columnised, a base_score helper\n- `src/slicer/cli.py` — `add`/`set` flags, `list --sort` and `--importance`/`--urgency` filters\n- `src/slicer/outline.py` — `importance:`/`urgency:` keys\n- `src/slicer/render.py` — a compact score/quadrant in the slice header or a column"
    },
    {
      "heading": "Failing tests",
      "body": "1. `add --importance 3 --urgency 1` stores 3/1; base score 31.\n2. An unset item defaults to 2/2, base score 22.\n3. `set --importance` / `--urgency` validate 1–3 and reject anything else.\n4. The outline `importance:`/`urgency:` keys import.\n5. `list --sort score` orders by base score, highest first; without it, order is unchanged.\n6. The score round-trips through to_dict/from_dict byte-identically."
    },
    {
      "heading": "Implement",
      "body": "Add `importance: int = 2`, `urgency: int = 2` to `Item`, serialized in the `fields`\nsub-dict in fixed key order. Validate 1–3 the way statuses validate their vocabulary —\nreject out of range, naming the range. Wire `--importance`/`--urgency` onto `add` and\n`set`; add the two outline keys; add `list --sort score` (a `sorted` over a copy, the\ntool's first computed item-sort, confined to the read-only list view so it never touches\nstored order or the byte-identity render check). Show the quadrant label where a reader\nsees the item.\n\n**Not in this slice:** dependency propagation and the `next` change — that is the\nfollowing slice, which builds on this one."
    },
    {
      "heading": "Check",
      "body": "`list --sort score` ranks the backlog; scores survive render/check byte-identical."
    },
    {
      "heading": "Git",
      "body": "One commit: importance/urgency on `Item` (model), a `_valid_score` guard and\nwiring in `ops`, the two outline keys, and `add`/`set` flags plus `list --sort\nscore` with a score/quadrant column in `cli`. Tests in `test_scoring.py`."
    }
  ],
  "notes": []
}
