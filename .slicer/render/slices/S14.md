<!-- GENERATED by slicer. Source: .slicer/slices/S14.json. Run `slicer render` after editing; `slicer check` fails if this file is stale. Do not edit. -->

# S14 — The TUI help line advertises a key that does something else

**Findings:** found auditing the machine interface · **Size: S** · **Tree:** slicer

## Why

`tui.HELP` reads `n new`, but `n` is wired to `ops.promote`. There is no way to create an
item from the TUI at all, so the string promises a feature that does not exist and
mislabels one that does.

## Files

- `src/slicer/tui.py` — `HELP`

## Failing tests

None worth writing for a string; the fix is one word.

## Implement

Change `n new` to `n promote`. Then decide separately whether the TUI should be able to
add an item — if it should, that is a different slice and this one just stops lying in
the meantime.

**Not in this slice:** adding items from the TUI.

## Check

Read the help line against the key table in `act`.

## Git
